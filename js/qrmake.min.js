$("#inputlink").bind("input", function() {
	$("#qrcode").html("");
	jQuery("#qrcode").qrcode({
		render:"image",
		width: 512,
		height: 512,
		ecLevel: 'L',
		minVersion: 1,
		maxVersion: 10,
		radius: 0.5,
		quiet: 2,
		text: $("#inputlink").val()
	});
});